services:
  workproof-studio:
    image: ghcr.io/ahmedsaturki/workproof-runtime:3.4.0-dev.5@sha256:88be340ace01e1d6d48f62af41c806048c41d3f6e3a20741fba29cdc70b8bd4f
    container_name: workproof-runtime
    restart: unless-stopped
    ports:
      - "127.0.0.1:8788:8788"
    volumes:
      - ./work-runs:/data/work-runs
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: "8788"
      WORK_DIRECTORY: /data/work-runs
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:8788/health').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 15s
