services:
  workproof-studio:
    image: ghcr.io/ahmedsaturki/workproof-runtime:3.5.0-dev.1@sha256:ab5b90eb3722b96d714f105536f5c4d6cdc18cebe22992c4fe758fbc88f547d7
    container_name: workproof-runtime
    restart: unless-stopped
    ports:
      - "127.0.0.1:8788:8788"
    volumes:
      - ./work-runs:/data/work-runs
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: "8788"
      WORK_DIRECTORY: /data/work-runs
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:8788/health').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 15s
