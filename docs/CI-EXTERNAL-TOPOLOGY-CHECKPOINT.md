# CI External Topology Checkpoint

This file exists only to place the candidate on the repository's push-tested `feature/**` CI path. It carries no runtime behavior and is safe to remove after verification.
